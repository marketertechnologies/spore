# spore age recipients - PUBLIC keys only, never private keys.
#
# Every key listed for a file in secrets.nix can decrypt that file. The
# .age files in this directory are encrypted to these recipients; the
# encrypted bytes are safe to commit, the plaintext never is.
#
# Two recipients for the rocky deploy:
#
#   operator   your age/ssh public key (so you can re-encrypt locally)
#   rocky      the host's ssh_host_ed25519 PUBLIC key (so the box can
#              decrypt at activation; agenix reads /etc/ssh/ssh_host_*)
#
# First-deploy flow (see docs/deploy.md):
#   1. After nixos-anywhere installs the box, grab its host pubkey:
#        ssh-keyscan -t ed25519 rocky
#   2. Replace the rocky placeholder below with that key.
#   3. agenix -e linear-api-key.age   # paste the real ROC token
#   4. colmena apply --on rocky
#
# The values below are PLACEHOLDERS so the tree builds before any box
# exists; replace both before the first real encrypt.

# operator
age1placeholderoperatorxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx

# rocky host key
ssh-ed25519 AAAAC3NzaC1lZDI1NTE5AAAAIPLACEHOLDERrockyhostkeyxxxxxxxxxxxxxxxxxxxxxx rocky
